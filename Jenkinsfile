pipeline {
    agent {docker {image 'registry.au-syd.bluemix.net/cindy_namespace/ubuntu:1' } }
    stages {
        stage('Build') {
		steps {
				sh 'ls -l'
                
            }
        }

    }
}
